# The current portal runs entirely in demo mode.
NEXT_PUBLIC_PORTAL_MODE=demo

# Future server-only control-plane connection. Never prefix secrets with
# NEXT_PUBLIC_ and never expose a customer CNWS API Key to browser JavaScript.
CNWS_CONTROL_PLANE_URL=
CNWS_CONTROL_PLANE_TOKEN=
